{
  "name": "test",
  "version": "1.0.0",
  "scripts": {
    "build": "lerna run --parallel build",
    "serve": "lerna run --parallel serve"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@vue/cli": "^5.0.0-beta.3",
    "lerna": "^5.1.8"
  }
}
